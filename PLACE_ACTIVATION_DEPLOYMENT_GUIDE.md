# 플레이스 활성화 기능 배포 가이드

## 📋 개요

플레이스 활성화 기능은 플레이스 진단 결과를 기반으로 매장의 활성화 상태를 추적하고 개선 방안을 제시하는 기능입니다.

## 🎯 주요 기능

### 1. 리뷰 추이 분석
- **방문자 리뷰**: 30일/7일 평균 추이 분석
- **블로그 리뷰**: 30일/7일 평균 추이 분석
- 증가/감소/안정 상태 표시

### 2. 답글 대기 관리
- 최근 300개 리뷰 기준 답글 대기 수 계산
- 가장 오래된 답글 대기 글 날짜 표시
- AI 답글생성 바로가기 링크

### 3. 플레이스 정보 관리
- **프로모션/쿠폰**: 현재 진행 중인 프로모션 개수
- **공지사항**: 최근 공지사항 날짜 및 업데이트 필요 여부
- **업체소개글**: SEO 최적화 생성 기능 (LLM 활용)
- **찾아오는길**: SEO 최적화 생성 기능 (LLM 활용)

### 4. SNS 및 웹사이트
- 홈페이지, 인스타그램, 블로그, 페이스북 등록 여부
- 미등록 시 등록 권장 메시지

### 5. 네이버 서비스
- 스마트콜, 네이버페이, 네이버예약, 네이버톡톡 사용 여부
- 미사용 시 설정 가이드 링크 제공

### 6. 요약 카드
- 개선이 필요한 항목 우선 표시
- 우선순위별 정렬 (high > medium > low)

## 📁 파일 구조

### 프론트엔드
```
frontend/
├── app/dashboard/naver/activation/
│   └── page.tsx                          # 플레이스 활성화 페이지
├── components/layout/
│   └── Sidebar.tsx                       # 사이드바 메뉴 추가
```

### 백엔드
```
backend/
├── app/routers/
│   └── naver.py                          # API 엔드포인트 추가
├── app/services/
│   └── naver_activation_service.py       # 활성화 서비스 로직
```

## 🚀 배포 방법

### 1. 프론트엔드 배포 (Vercel)

#### GitHub Desktop 사용
```bash
# 1. GitHub Desktop에서 변경사항 확인
#    - frontend/app/dashboard/naver/activation/page.tsx (신규)
#    - frontend/components/layout/Sidebar.tsx (수정)

# 2. 커밋 메시지 작성
feat: 플레이스 활성화 기능 추가
- 리뷰 추이 분석 (30일/7일 평균)
- 답글 대기 수 계산 및 관리
- 프로모션, 공지사항 현황 표시
- 업체소개글/찾아오는길 SEO 최적화 생성 (LLM)
- SNS 및 네이버 서비스 사용 여부 체크
- 개선 필요 항목 우선 표시 (요약 카드)
- Mantine 7 기반 UI 구현

# 3. Commit to main
# 4. Push origin
# 5. Vercel 자동 배포 확인 (약 2-3분 소요)
```

### 2. 백엔드 배포 (AWS EC2)

#### SSH 접속
```bash
# Windows PowerShell에서 실행
ssh -i "C:\Users\smbae\Downloads\egurado keyfair.pem" ubuntu@3.34.136.255
```

#### 배포 스크립트
```bash
# 1. 백엔드 디렉토리로 이동
cd ~/egurado/backend

# 2. Git Pull
git pull origin main

# 3. Docker 컨테이너 재시작
docker-compose down
docker-compose up -d --build

# 4. 로그 확인
docker-compose logs -f egurado-api

# 5. 정상 작동 확인
# [플레이스 활성화] 로그가 출력되는지 확인
# Ctrl+C로 로그 종료
```

## 🧪 테스트 체크리스트

### 프론트엔드 테스트
- [ ] 사이드바에 "플레이스 활성화" 메뉴 표시
- [ ] 매장 선택 드롭다운 정상 작동
- [ ] 활성화 정보 로딩 및 표시
- [ ] 요약 카드 (이슈) 표시
- [ ] 리뷰 추이 분석 표시 (방문자/블로그)
- [ ] 답글 대기 수 표시
- [ ] 프로모션/공지사항 현황 표시
- [ ] 업체소개글 생성 모달 작동
- [ ] 찾아오는길 생성 모달 작동
- [ ] SNS 및 웹사이트 현황 표시
- [ ] 네이버 서비스 현황 표시
- [ ] 반응형 디자인 (모바일/태블릿/PC)

### 백엔드 테스트
- [ ] GET /api/v1/naver/activation/{store_id} 정상 응답
- [ ] POST /api/v1/naver/activation/generate-description 정상 작동
- [ ] POST /api/v1/naver/activation/generate-directions 정상 작동
- [ ] 권한 확인 (다른 사용자의 매장 접근 불가)
- [ ] 진단 히스토리 데이터 활용 (리뷰 추이)
- [ ] 답글 대기 수 계산 (최근 300개 리뷰)
- [ ] 로그 출력 확인

## 📊 API 명세

### 1. 활성화 정보 조회
```http
GET /api/v1/naver/activation/{store_id}
Authorization: Bearer {access_token}

Response:
{
  "status": "success",
  "data": {
    "store_name": "매장명",
    "place_id": "플레이스ID",
    "visitor_review_count": 245,
    "visitor_review_trend_30d": {
      "average": 8.5,
      "change_percentage": 12.3,
      "direction": "up"
    },
    "visitor_review_trend_7d": {
      "average": 10.2,
      "change_percentage": 5.7,
      "direction": "up"
    },
    "pending_reply_count": 15,
    "oldest_pending_review_date": "2026-01-20",
    "blog_review_count": 128,
    "blog_review_trend_30d": {...},
    "blog_review_trend_7d": {...},
    "has_promotion": true,
    "promotion_count": 2,
    "has_announcement": true,
    "announcement_count": 3,
    "last_announcement_date": "2026-01-25",
    "days_since_last_announcement": 2,
    "description": "업체소개글 내용",
    "directions": "찾아오는길 내용",
    "homepage": "https://...",
    "instagram": "https://...",
    "facebook": null,
    "blog": "https://...",
    "has_smart_call": true,
    "has_naver_pay": false,
    "has_naver_booking": true,
    "has_naver_talk": false,
    "issues": [
      {
        "category": "답글 대기",
        "severity": "high",
        "message": "15개의 리뷰에 답글이 없습니다",
        "action": "AI 답글생성을 이용해서 빠르게 답글을 작성하세요"
      },
      ...
    ]
  }
}
```

### 2. 업체소개글 생성
```http
POST /api/v1/naver/activation/generate-description
Authorization: Bearer {access_token}
Content-Type: application/json

Request:
{
  "store_id": "uuid",
  "prompt": "강남역 근처 프리미엄 일식당, 신선한 재료와 정통 일본식 조리법 강조"
}

Response:
{
  "status": "success",
  "generated_text": "강남역 11번 출구 도보 3분 거리에 위치한 프리미엄 일식당입니다..."
}
```

### 3. 찾아오는길 생성
```http
POST /api/v1/naver/activation/generate-directions
Authorization: Bearer {access_token}
Content-Type: application/json

Request:
{
  "store_id": "uuid",
  "prompt": "지하철 2호선 강남역 11번 출구에서 도보 3분, 주차 가능"
}

Response:
{
  "status": "success",
  "generated_text": "지하철 2호선 강남역 11번 출구로 나오셔서..."
}
```

## 🔧 기술 스택

### 프론트엔드
- **Framework**: Next.js 14 (App Router)
- **UI Library**: Mantine 7
- **Icons**: Lucide React
- **State Management**: React Hooks
- **Styling**: Tailwind CSS + Mantine CSS

### 백엔드
- **Framework**: FastAPI
- **Database**: Supabase (PostgreSQL)
- **LLM**: OpenAI GPT-4 (via llm_reply_service)
- **Services**:
  - `naver_complete_diagnosis_service`: 플레이스 진단 정보
  - `naver_review_service`: 리뷰 조회
  - `llm_reply_service`: LLM 텍스트 생성

## 📝 주의사항

### 1. 진단 히스토리 의존성
- 리뷰 추이 분석은 진단 히스토리 데이터를 활용합니다
- 진단 히스토리가 2개 이상 있어야 추이 분석이 가능합니다
- 데이터가 부족한 경우 기본값(0, stable) 반환

### 2. LLM 비용
- 업체소개글/찾아오는길 생성은 LLM API를 호출합니다
- 사용자가 생성 버튼을 클릭할 때만 호출됩니다
- 크레딧 차감 로직은 별도로 구현 필요 (향후 개선)

### 3. 답글 대기 수 계산
- 최근 300개 리뷰만 조회합니다 (API 제한)
- 리뷰가 300개 미만인 경우 전체 리뷰 대상

### 4. 네이버 서비스 감지
- 플레이스 진단 데이터를 기반으로 감지합니다
- 일부 서비스는 감지가 불완전할 수 있습니다

## 🐛 트러블슈팅

### 문제: 활성화 정보 조회 실패
**증상**: API 호출 시 500 에러
**해결**:
1. 백엔드 로그 확인: `docker-compose logs -f egurado-api`
2. 플레이스 진단 서비스 정상 작동 확인
3. 매장의 place_id 및 store_name 확인

### 문제: 리뷰 추이가 표시되지 않음
**증상**: 모든 추이가 0% / stable
**해결**:
1. 진단 히스토리 데이터 확인: `SELECT * FROM diagnosis_history WHERE store_id = '...'`
2. 최소 2개 이상의 진단 히스토리 필요
3. 플레이스 진단을 2회 이상 실행

### 문제: LLM 생성 실패
**증상**: 업체소개글/찾아오는길 생성 시 오류
**해결**:
1. OpenAI API 키 확인
2. llm_reply_service 정상 작동 확인
3. 프롬프트 길이 확인 (너무 길면 실패 가능)

## 📈 향후 개선 사항

### 1. 크레딧 차감
- LLM 생성 시 크레딧 차감 로직 추가
- 생성 전 크레딧 잔액 확인

### 2. 히스토리 차트
- 리뷰 추이를 그래프로 시각화
- Recharts 또는 Chart.js 활용

### 3. 알림 기능
- 답글 대기 수가 일정 개수 이상일 때 알림
- 공지사항 업데이트 주기 알림

### 4. 자동 진단
- 주기적으로 플레이스 진단 실행
- 진단 히스토리 자동 축적

### 5. 경쟁사 비교
- 동일 카테고리 경쟁사와 활성화 지표 비교

## ✅ 배포 완료 확인

배포 완료 후 다음 사항을 확인하세요:

1. **프론트엔드**
   - [ ] https://whiplace.com/dashboard/naver/activation 접속 가능
   - [ ] 사이드바 메뉴 표시
   - [ ] 매장 선택 및 정보 조회

2. **백엔드**
   - [ ] API 엔드포인트 정상 응답
   - [ ] 로그 출력 확인
   - [ ] 에러 없이 작동

3. **기능 테스트**
   - [ ] 리뷰 추이 분석
   - [ ] 답글 대기 수
   - [ ] LLM 생성 기능

## 📞 문의

문제 발생 시:
1. 백엔드 로그 확인
2. 브라우저 콘솔 확인
3. API 응답 확인

---

**작성일**: 2026-01-27  
**작성자**: AI Assistant  
**버전**: 1.0.0
